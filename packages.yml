packages:
  # Used by ccsr models: pretty_time, get_column_values, group_by.
  - package: dbt-labs/dbt_utils
    version: [">=1.2.0", "<2.0.0"]

  # the_tuva_project (tuva-core) provides load_package_seed (used by the seed
  # post-hooks in dbt_project.yml), the cross-database function layer
  # (bool_and_agg / bool_or_agg) and the core__* models that ccsr refs.
  # Pinned to the reviewed tuva-core architecture commit because the required
  # cross-database helpers are not in a tagged tuva-core release yet.
  # TODO(D2): switch to hub range [">=1.0.0", "<1.1.0"]
  - git: "https://github.com/tuva-health/tuva-core.git"
    revision: c14dc2c86545f5c9c49347c43ba683ebed299311
